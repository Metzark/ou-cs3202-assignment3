
def sumArray(arr):
    sum = 0;
    for i in range(0, len(arr)):
        sum += arr[i]
    return sum

def productArray(arr):
    product = 1;
    for i in range(0, len(arr)):
        product *= arr[i]
    return product

